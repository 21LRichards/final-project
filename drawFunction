draw = function() {
    if (currentScene === 0) {
    splash();
    }
    else if (currentScene === 1) {
    howToPlay();
    }
    else if (currentScene === 2) {
    noLoop();
    gameScreen();
    }
    if (Score===0) {
    textSize(30);
    text("Game Over",117,144);
    }
};
